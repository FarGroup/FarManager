# fileowner.cpp
## English
### Purpose
This file implements functionality related to: Кэш SID`ов и функция GetOwner
Main functional areas: file system operations
### Key Classes
- `sid`: Implements file system operations
- `sid_hash_eq`: Implements file system operations
### Key Functions
- `Callable()`
- `EqualSid()`
- `GetFileOwner()`
- `IsOwned()`
- `ProcessFileOwner()`
- `SetFileOwner()`
- `SetOwnerInternal()`
- `Sid()`
- `SidToName()`
- `SidToNameCached()`
### Summary
The `fileowner.cpp` file provides essential functionality for file system operations. It defines 2 class(es) and contains approximately 17 function(s) to support the Far Manager file manager operations.

---

## Polski (Polish)
### Cel
Ten plik implementuje funkcjonalność związaną z: Кэш SID`ов и функция GetOwner
Główne obszary funkcjonalne: operacje systemowyu plików
### Kluczowe Klasy
- `sid`: Implementuje operacje systemowyu plików
- `sid_hash_eq`: Implementuje operacje systemowyu plików
### Kluczowe Funkcje
- `Callable()`
- `EqualSid()`
- `GetFileOwner()`
- `IsOwned()`
- `ProcessFileOwner()`
- `SetFileOwner()`
- `SetOwnerInternal()`
- `Sid()`
- `SidToName()`
- `SidToNameCached()`
### Podsumowanie
Plik `fileowner.cpp` zapewnia podstawową funkcjonalność dla operacje systemowyu plików. Definiuje 2 klas(y) i zawiera około 17 funkcji wspierających operacje menedżera plików Far Manager.
