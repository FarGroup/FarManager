# RegExp.cpp
## English
### Purpose
This file implements functionality related to: Regular expressions
Main functional areas: regular expressions
### Key Classes
- `REOpCode_data`: Implements regular expressions
- `RegExp`: Implements regular expressions
- `SAlternative`: Implements regular expressions
- `SAssert`: Implements regular expressions
- `SBracket`: Implements regular expressions
- `SNamedBracket`: Implements regular expressions
- `SRange`: Implements regular expressions
- `Setter`: Implements regular expressions
- `tests`: Implements regular expressions
### Key Functions
- `isType()`: Tests whether Type condition is true or property exists
- `Reset()`: Executes Reset operation as part of regular expressions
- `bool()`: Executes bool operation as part of regular expressions
- `GetBit()`: Retrieves Bit from internal state or data structure
- `Setter()`: Updates ter in internal state or configuration
- `SetBit()`: Updates Bit in internal state or configuration
- `ClearBit()`: Clears all data and resets to initial empty state
- `regex_exception()`: Executes regex_exception operation as part of regular expressions
### Summary
The `RegExp.cpp` file provides essential functionality for regular expressions. It defines 9 class(es) and contains approximately 8 function(s) to support the Far Manager file manager operations.

---

## Polski (Polish)
### Cel
Ten plik implementuje funkcjonalność związaną z: Regular expressions
Główne obszary funkcjonalne: wyrażenia regularne
### Kluczowe Klasy
- `REOpCode_data`: Implementuje wyrażenia regularne
- `RegExp`: Implementuje wyrażenia regularne
- `SAlternative`: Implementuje wyrażenia regularne
- `SAssert`: Implementuje wyrażenia regularne
- `SBracket`: Implementuje wyrażenia regularne
- `SNamedBracket`: Implementuje wyrażenia regularne
- `SRange`: Implementuje wyrażenia regularne
- `Setter`: Implementuje wyrażenia regularne
- `tests`: Implementuje wyrażenia regularne
### Kluczowe Funkcje
- `isType()`: Testuje czy Type warunek jest prawdziwy lub właściwość istnieje
- `Reset()`: Wykonuje Reset operację jako część wyrażenia regularne
- `bool()`: Wykonuje bool operację jako część wyrażenia regularne
- `GetBit()`: Pobiera Bit ze stanu wewnętrznego lub struktury danych
- `Setter()`: Aktualizuje ter w stanie wewnętrznym lub konfiguracji
- `SetBit()`: Aktualizuje Bit w stanie wewnętrznym lub konfiguracji
- `ClearBit()`: Czyści wszystkie dane i resetuje do początkowego pustego stanu
- `regex_exception()`: Wykonuje regex_exception operację jako część wyrażenia regularne
### Podsumowanie
Plik `RegExp.cpp` zapewnia podstawową funkcjonalność dla wyrażenia regularne. Definiuje 9 klas(y) i zawiera około 8 funkcji wspierających operacje menedżera plików Far Manager.
