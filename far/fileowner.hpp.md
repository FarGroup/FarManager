# fileowner.hpp
## English
### Purpose
This file implements functionality related to: Кэш SID`ов и функция GetOwner
Main functional areas: file system operations
### Key Functions
- `GetFileOwner()`: Retrieves FileOwner from internal state or data structure
- `SetFileOwner()`: Updates FileOwner in internal state or configuration
- `SetOwnerInternal()`: Updates OwnerInternal in internal state or configuration
### Summary
The `fileowner.hpp` file provides essential functionality for file system operations. and contains approximately 3 function(s) to support the Far Manager file manager operations.

---

## Polski (Polish)
### Cel
Ten plik implementuje funkcjonalność związaną z: Кэш SID`ов и функция GetOwner
Główne obszary funkcjonalne: operacje systemu plików
### Kluczowe Funkcje
- `GetFileOwner()`: Pobiera FileOwner ze stanu wewnętrznego lub struktury danych
- `SetFileOwner()`: Aktualizuje FileOwner w stanie wewnętrznym lub konfiguracji
- `SetOwnerInternal()`: Aktualizuje OwnerInternal w stanie wewnętrznym lub konfiguracji
### Podsumowanie
Plik `fileowner.hpp` zapewnia podstawową funkcjonalność dla operacje systemu plików. i zawiera około 3 funkcji wspierających operacje menedżera plików Far Manager.
