# fileview.hpp
## English
### Purpose
This file implements functionality related to: Просмотр файла - надстройка над viewer.cpp
Main functional areas: file viewing, file system operations
### Key Classes
- `f3_key_timer`: Implements file viewing, file system operations
- `private_tag`: Implements file viewing, file system operations
- `reload_timer`: Implements file viewing, file system operations
### Key Functions
- `CanFastHide()`
- `DisplayObject()`
- `GetById()`
- `GetId()`
- `GetTitle()`
- `GetType()`
- `GetViewFilePos()`
- `GetViewFileSize()`
- `GetViewer()`
- `InitKeyBar()`
### Summary
The `fileview.hpp` file provides essential functionality for file viewing, file system operations. It defines 3 class(es) and contains approximately 20 function(s) to support the Far Manager file manager operations.

---

## Polski (Polish)
### Cel
Ten plik implementuje funkcjonalność związaną z: Просмотр файла - надстройка над viewer.cpp
Główne obszary funkcjonalne: przeglądanie plików, operacje systemowyu plików
### Kluczowe Klasy
- `f3_key_timer`: Implementuje przeglądanie plików, operacje systemowyu plików
- `private_tag`: Implementuje przeglądanie plików, operacje systemowyu plików
- `reload_timer`: Implementuje przeglądanie plików, operacje systemowyu plików
### Kluczowe Funkcje
- `CanFastHide()`
- `DisplayObject()`
- `GetById()`
- `GetId()`
- `GetTitle()`
- `GetType()`
- `GetViewFilePos()`
- `GetViewFileSize()`
- `GetViewer()`
- `InitKeyBar()`
### Podsumowanie
Plik `fileview.hpp` zapewnia podstawową funkcjonalność dla przeglądanie plików, operacje systemowyu plików. Definiuje 3 klas(y) i zawiera około 20 funkcji wspierających operacje menedżera plików Far Manager.
