# filefilterparams.hpp
## English
### Purpose
This file implements functionality related to: Параметры Файлового фильтра
Main functional areas: file system operations
### Key Classes
- `FileFilterParams`: Implements file system operations
- `filter_area`: Implements file system operations
- `filter_dates`: Implements file system operations
- `fmask`: Implements file system operations
### Key Functions
- `filter_dates()`: Performs specific operation
- `bool()`: Performs specific operation
- `Clone()`: Performs specific operation
- `SetTitle()`: Sets or assigns data
- `SetMask()`: Sets or assigns data
- `SetDate()`: Sets or assigns data
- `SetSize()`: Sets or assigns data
- `SetHardLinks()`: Sets or assigns data
- `SetAttr()`: Sets or assigns data
- `SetColors()`: Sets or assigns data
### Summary
The `filefilterparams.hpp` file provides essential functionality for file system operations. It defines 4 class(es) and contains approximately 10 function(s) to support the Far Manager file manager operations.

---

## Polski (Polish)
### Cel
Ten plik implementuje funkcjonalność związaną z: Параметры Файлового фильтра
Główne obszary funkcjonalne: operacje systemu plików
### Kluczowe Klasy
- `FileFilterParams`: Implementuje operacje systemu plików
- `filter_area`: Implementuje operacje systemu plików
- `filter_dates`: Implementuje operacje systemu plików
- `fmask`: Implementuje operacje systemu plików
### Kluczowe Funkcje
- `filter_dates()`: Wykonuje specyficzną operację
- `bool()`: Wykonuje specyficzną operację
- `Clone()`: Wykonuje specyficzną operację
- `SetTitle()`: Ustawia lub przypisuje dane
- `SetMask()`: Ustawia lub przypisuje dane
- `SetDate()`: Ustawia lub przypisuje dane
- `SetSize()`: Ustawia lub przypisuje dane
- `SetHardLinks()`: Ustawia lub przypisuje dane
- `SetAttr()`: Ustawia lub przypisuje dane
- `SetColors()`: Ustawia lub przypisuje dane
### Podsumowanie
Plik `filefilterparams.hpp` zapewnia podstawową funkcjonalność dla operacje systemu plików. Definiuje 4 klas(y) i zawiera około 10 funkcji wspierających operacje menedżera plików Far Manager.
