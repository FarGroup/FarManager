# fileedit.cpp
## English
### Purpose
This file implements functionality related to: Редактирование файла - надстройка над editor.cpp
Main functional areas: text editing, file system operations
### Key Classes
- `FileEditor`: Implements text editing, file system operations
### Key Functions
- `EditFile()`
- `EnumFileLines()`
- `InitKeyBar()`
- `ReProcessKey()`
- `SCOPED_ACTION()`
- `SaveAction()`
- `SaveToCache()`
- `SetExitCode()`
- `ShowConsoleTitle()`
- `Stream()`
### Summary
The `fileedit.cpp` file provides essential functionality for text editing, file system operations. It defines 1 class(es) and contains approximately 20 function(s) to support the Far Manager file manager operations.

---

## Polski (Polish)
### Cel
Ten plik implementuje funkcjonalność związaną z: Редактирование файла - надстройка над editor.cpp
Główne obszary funkcjonalne: edycja tekstu, operacje systemowyu plików
### Kluczowe Klasy
- `FileEditor`: Implementuje edycja tekstu, operacje systemowyu plików
### Kluczowe Funkcje
- `EditFile()`
- `EnumFileLines()`
- `InitKeyBar()`
- `ReProcessKey()`
- `SCOPED_ACTION()`
- `SaveAction()`
- `SaveToCache()`
- `SetExitCode()`
- `ShowConsoleTitle()`
- `Stream()`
### Podsumowanie
Plik `fileedit.cpp` zapewnia podstawową funkcjonalność dla edycja tekstu, operacje systemowyu plików. Definiuje 1 klas(y) i zawiera około 20 funkcji wspierających operacje menedżera plików Far Manager.
