# strmix.hpp
## English
### Purpose
This file implements functionality related to: Куча разных вспомогательных функций по работе со строками
Main functional areas: string utilities
### Key Classes
- `RegExp`: Implements string utilities
- `RegExpMatch`: Implements string utilities
- `regex_match`: Implements string utilities
### Key Functions
- `BlobToHexString()`
- `CheckFileSizeStringFormat()`
- `ConvertHexString()`
- `FileSizeToStr()`
- `FlagsToString()`
- `GroupDigitsInvariant()`
- `HexMask()`
- `HexStringToBlob()`
- `HexToInt()`
- `SearchAndReplaceString()`
### Namespaces
- `detail`
- `inplace`
- `legacy`
### Summary
The `strmix.hpp` file provides essential functionality for string utilities. It defines 3 class(es) and contains approximately 20 function(s) to support the Far Manager file manager operations.

---

## Polski (Polish)
### Cel
Ten plik implementuje funkcjonalność związaną z: Куча разных вспомогательных функций по работе со строками
Główne obszary funkcjonalne: narzędzia łańcuchowe
### Kluczowe Klasy
- `RegExp`: Implementuje narzędzia łańcuchowe
- `RegExpMatch`: Implementuje narzędzia łańcuchowe
- `regex_match`: Implementuje narzędzia łańcuchowe
### Kluczowe Funkcje
- `BlobToHexString()`
- `CheckFileSizeStringFormat()`
- `ConvertHexString()`
- `FileSizeToStr()`
- `FlagsToString()`
- `GroupDigitsInvariant()`
- `HexMask()`
- `HexStringToBlob()`
- `HexToInt()`
- `SearchAndReplaceString()`
### Przestrzenie nazw
- `detail`
- `inplace`
- `legacy`
### Podsumowanie
Plik `strmix.hpp` zapewnia podstawową funkcjonalność dla narzędzia łańcuchowe. Definiuje 3 klas(y) i zawiera około 20 funkcji wspierających operacje menedżera plików Far Manager.
