# strmix.cpp
## English
### Purpose
This file implements functionality related to: Куча разных вспомогательных функций по работе со строками
Main functional areas: string utilities
### Key Classes
- `size`: Implements string utilities
- `tests`: Implements string utilities
- `units`: Implements string utilities
### Key Functions
- `BlobToHexString()`
- `CanContainWholeWord()`
- `CheckFileSizeStringFormat()`
- `ConvertHexString()`
- `FileSizeToStr()`
- `FileSizeToStrImpl()`
- `GroupDigitsInvariant()`
- `HexMask()`
- `HexStringToBlob()`
- `HexToInt()`
### Namespaces
- `id`
### Summary
The `strmix.cpp` file provides essential functionality for string utilities. It defines 3 class(es) and contains approximately 20 function(s) to support the Far Manager file manager operations.

---

## Polski (Polish)
### Cel
Ten plik implementuje funkcjonalność związaną z: Куча разных вспомогательных функций по работе со строками
Główne obszary funkcjonalne: narzędzia łańcuchowe
### Kluczowe Klasy
- `size`: Implementuje narzędzia łańcuchowe
- `tests`: Implementuje narzędzia łańcuchowe
- `units`: Implementuje narzędzia łańcuchowe
### Kluczowe Funkcje
- `BlobToHexString()`
- `CanContainWholeWord()`
- `CheckFileSizeStringFormat()`
- `ConvertHexString()`
- `FileSizeToStr()`
- `FileSizeToStrImpl()`
- `GroupDigitsInvariant()`
- `HexMask()`
- `HexStringToBlob()`
- `HexToInt()`
### Przestrzenie nazw
- `id`
### Podsumowanie
Plik `strmix.cpp` zapewnia podstawową funkcjonalność dla narzędzia łańcuchowe. Definiuje 3 klas(y) i zawiera około 20 funkcji wspierających operacje menedżera plików Far Manager.
