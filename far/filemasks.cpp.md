# filemasks.cpp
## English
### Purpose
This file implements functionality related to: Класс для работы с масками файлов (учитывается наличие масок исключения).
Main functional areas: file system operations
### Key Classes
- `filemasks`: Implements file system operations
- `regex_data`: Implements file system operations
- `with_brackets`: Implements file system operations
### Key Functions
- `extract_impl()`: Executes extract_impl operation as part of file system operations
- `extract_separators()`: Executes extract_separators operation as part of file system operations
- `extract_masks()`: Executes extract_masks operation as part of file system operations
- `extract_re()`: Executes extract_re operation as part of file system operations
- `assign()`: Executes assign operation as part of file system operations
- `empty()`: Executes empty operation as part of file system operations
- `last_matches()`: Executes last_matches operation as part of file system operations
- `get_mask_group()`: Retrieves _mask_group from internal state or data structure
- `ExpandedGroups()`: Executes ExpandedGroups operation as part of file system operations
- `add_pathext()`: Adds new _pathext to collection or list
### Summary
The `filemasks.cpp` file provides essential functionality for file system operations. It defines 3 class(es) and contains approximately 10 function(s) to support the Far Manager file manager operations.

---

## Polski (Polish)
### Cel
Ten plik implementuje funkcjonalność związaną z: Класс для работы с масками файлов (учитывается наличие масок исключения).
Główne obszary funkcjonalne: operacje systemu plików
### Kluczowe Klasy
- `filemasks`: Implementuje operacje systemu plików
- `regex_data`: Implementuje operacje systemu plików
- `with_brackets`: Implementuje operacje systemu plików
### Kluczowe Funkcje
- `extract_impl()`: Wykonuje extract_impl operację jako część operacje systemu plików
- `extract_separators()`: Wykonuje extract_separators operację jako część operacje systemu plików
- `extract_masks()`: Wykonuje extract_masks operację jako część operacje systemu plików
- `extract_re()`: Wykonuje extract_re operację jako część operacje systemu plików
- `assign()`: Wykonuje assign operację jako część operacje systemu plików
- `empty()`: Wykonuje empty operację jako część operacje systemu plików
- `last_matches()`: Wykonuje last_matches operację jako część operacje systemu plików
- `get_mask_group()`: Pobiera _mask_group ze stanu wewnętrznego lub struktury danych
- `ExpandedGroups()`: Wykonuje ExpandedGroups operację jako część operacje systemu plików
- `add_pathext()`: Dodaje nowy _pathext do kolekcji lub listy
### Podsumowanie
Plik `filemasks.cpp` zapewnia podstawową funkcjonalność dla operacje systemu plików. Definiuje 3 klas(y) i zawiera około 10 funkcji wspierających operacje menedżera plików Far Manager.
