# filefilter.cpp
## English
### Purpose
This file implements functionality related to: Файловый фильтр
Main functional areas: file system operations
### Key Functions
- `FileInFilter()`
- `FilterData()`
- `GetCheck()`
- `Key()`
- `LoadAreaFlags()`
- `LoadLegacyAreaFlags()`
- `ParseAndAddMasks()`
- `Process()`
- `ProcessSelection()`
- `SaveFlags()`
### Namespaces
- `legacy_names`
- `names`
### Summary
The `filefilter.cpp` file provides essential functionality for file system operations. and contains approximately 14 function(s) to support the Far Manager file manager operations.

---

## Polski (Polish)
### Cel
Ten plik implementuje funkcjonalność związaną z: Файловый фильтр
Główne obszary funkcjonalne: operacje systemowyu plików
### Kluczowe Funkcje
- `FileInFilter()`
- `FilterData()`
- `GetCheck()`
- `Key()`
- `LoadAreaFlags()`
- `LoadLegacyAreaFlags()`
- `ParseAndAddMasks()`
- `Process()`
- `ProcessSelection()`
- `SaveFlags()`
### Przestrzenie nazw
- `legacy_names`
- `names`
### Podsumowanie
Plik `filefilter.cpp` zapewnia podstawową funkcjonalność dla operacje systemowyu plików. i zawiera około 14 funkcji wspierających operacje menedżera plików Far Manager.
