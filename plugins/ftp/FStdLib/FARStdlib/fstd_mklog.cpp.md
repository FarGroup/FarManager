# fstd_mklog.cpp
## English
### Purpose
Main functional areas: file system operations
### Key Functions
- `LOGInit()`: Executes LOGInit operation as part of file system operations
- `FP_GetLogFullFileName()`: Executes FP_GetLogFullFileName operation as part of file system operations
- `strcpy()`: Executes strcpy operation as part of file system operations
### Summary
The `fstd_mklog.cpp` file provides essential functionality for file system operations. and contains approximately 3 function(s) to support the Far Manager file manager operations.

---

## Polski (Polish)
### Cel
Główne obszary funkcjonalne: operacje systemu plików
### Kluczowe Funkcje
- `LOGInit()`: Wykonuje LOGInit operację jako część operacje systemu plików
- `FP_GetLogFullFileName()`: Wykonuje FP_GetLogFullFileName operację jako część operacje systemu plików
- `strcpy()`: Wykonuje strcpy operację jako część operacje systemu plików
### Podsumowanie
Plik `fstd_mklog.cpp` zapewnia podstawową funkcjonalność dla operacje systemu plików. i zawiera około 3 funkcji wspierających operacje menedżera plików Far Manager.
