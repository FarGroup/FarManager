# fstd_Arg.cpp
## English
### Purpose
Main functional areas: file system operations
### Key Functions
- `CTArgInit()`: Executes CTArgInit operation as part of file system operations
- `CTArgGet()`: Executes CTArgGet operation as part of file system operations
- `CTArgGetArg()`: Executes CTArgGetArg operation as part of file system operations
- `CTArgCheck()`: Executes CTArgCheck operation as part of file system operations
### Summary
The `fstd_Arg.cpp` file provides essential functionality for file system operations. and contains approximately 4 function(s) to support the Far Manager file manager operations.

---

## Polski (Polish)
### Cel
Główne obszary funkcjonalne: operacje systemu plików
### Kluczowe Funkcje
- `CTArgInit()`: Wykonuje CTArgInit operację jako część operacje systemu plików
- `CTArgGet()`: Wykonuje CTArgGet operację jako część operacje systemu plików
- `CTArgGetArg()`: Wykonuje CTArgGetArg operację jako część operacje systemu plików
- `CTArgCheck()`: Wykonuje CTArgCheck operację jako część operacje systemu plików
### Podsumowanie
Plik `fstd_Arg.cpp` zapewnia podstawową funkcjonalność dla operacje systemu plików. i zawiera około 4 funkcji wspierających operacje menedżera plików Far Manager.
