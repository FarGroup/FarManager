-- Хотелось бы чтобы работало копирование папок, включающие пустые
   папки с одного фтп на другой;
   Насчет последнего глюка: он происходит, когда в куче выделенных папок
   для копирования, одна из папок пустая

-- add size info to queue items;
   use size info to draw progress;
   check why fail first LIST fail;

-- queue: selection; group move, delete, run

-- try WinINET possibilities for use to ftp client;

-- SSL

-- FilesList -> queue

-- save|load lists in queue

-- "Vasily Titsky" <winner@tsure.ru>
   do not scan directories before copy

-- OPT speaker|sound beeper

-- Sysop message
   <- 214 Message from sysop: sdfsfd

-- FileFind
   D> Подключаемся по фтп к серверу, по ALT-F7 ищем строку текста в файлах,
   D> находит, пытаемся нажать F3 или F4 получаем фиг, сразу после этого из
   D> панели найденных файлов по вводу пытаемся перейти на файл - фиг.
   D> Хотя если сразу после поиска перейти на файл то все работает.
   --
   И> тогда пускай не дает смотреть ... или предупреждает , что поиск будет
   И> остановлен ... ну если не сложно ;)

-- sht@abgcard.ru
   sar>   Для исключения молчаливого успешного копирования при ошибочном
   sar> формировании имени переписываемого файла (при неправильном определении
   sar> таблицы кодировки) следует помнить о кол-ве запрошенных байт файла и если
   sar> принятое кол-во = 0, то уточнить что делать дальше в виде:
   sar>                             Предупреждение
   sar>           Принятый файл имеет отличную длину
   sar>                                   File_Name
   sar>    Исходный      NNNNNNN      DD.MM.YY MM:HH:CC
   sar>    Принятый                    0       DD.MM.YY MM:HH:CC
   sar>      Продолжить   Не сохранять    Отменить операцию
   sar> ожидать некоторое время и продолжить операцию копирования. После завершения
   sar> операции:
   sar>                   Предупреждение
   sar> Некоторые файлы имеют отличную длину
   sar>                              Ok
   sar> Ситуация: 1)определили имя файла с ошибкой 2) дали команду RETR #$%%^ 3)
   sar> сервер файла не нашел 4) содержимого не передал 5) но сервер сказал Ok (или
   sar> Ok и затем Err) 6) сохраняется пустой файл с именем #$%%^ без состояния
   sar> ошибки 7) продолжаем работать дальше как ни в чем не бывало. 8) Как
   sar> результат - данные утеряны.

-- хочу настройку сортировки (можно глобальную и отдельную для каждого хоста)

-- "Ctrl+", "Ctrl-", "*" in file selection list

-- Wesha the Leopard <wesha@mail.ru>
   1) При копировании не копирует пустые каталоги

-- convert %20 to characters in urls

-- Roman Medvedev <rvm@echo.ru>
   RM> Просто показываем дерево прокэшированных каталогов для соединения.
   RM> Возможность пробежать и по ентеру войти в нужный каталог(следует CWD, list и
   RM> т.д.)
