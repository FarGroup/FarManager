ifndef WIDE
ALLDIRS = align autowrap brackets compare editcase farcmds filecase \
	  hlfviewer macroview network proclist tmppanel \
          drawline ftp multiarc
else
ALLDIRS = align autowrap brackets compare editcase farcmds filecase \
	  hlfviewer macroview network proclist tmppanel
endif

all: libcrt $(ALLDIRS)

FICTIVE=_fictive_
$(FICTIVE):

libcrt: $(FICTIVE)
	@echo ===========CRT library
	@$(MAKE) -C common/CRT -f makefile_lib_gcc

$(ALLDIRS): $(FICTIVE)
	@echo ===========$@
	@$(MAKE) -C $@ -f makefile_gcc
