# FarDialogs.hpp
## English
### Purpose
Main functional areas: dialog handling
### Key Classes
- `FarDialog`: Implements dialog handling
- `FarDialogHandler`: Implements dialog handling
- `FarPagedDialog`: Implements dialog handling
- `FarPagedDialogHandler`: Implements dialog handling
- `PageInfo`: Implements dialog handling
### Key Functions
- `SetFlags()`: Updates Flags in internal state or configuration
- `SetDialogFlags()`: Updates DialogFlags in internal state or configuration
- `GetDialogFlags()`: Retrieves DialogFlags from internal state or data structure
- `FirstButton()`: Executes FirstButton operation as part of dialog handling
- `SetFocus()`: Updates Focus in internal state or configuration
- `DefaultButton()`: Executes DefaultButton operation as part of dialog handling
- `Control()`: Executes Control operation as part of dialog handling
- `SingleBox()`: Executes SingleBox operation as part of dialog handling
- `DoubleBox()`: Executes DoubleBox operation as part of dialog handling
- `Edit()`: Executes Edit operation as part of dialog handling
### Summary
The `FarDialogs.hpp` file provides essential functionality for dialog handling. It defines 5 class(es) and contains approximately 10 function(s) to support the Far Manager file manager operations.

---

## Polski (Polish)
### Cel
Główne obszary funkcjonalne: obsługa dialogów
### Kluczowe Klasy
- `FarDialog`: Implementuje obsługa dialogów
- `FarDialogHandler`: Implementuje obsługa dialogów
- `FarPagedDialog`: Implementuje obsługa dialogów
- `FarPagedDialogHandler`: Implementuje obsługa dialogów
- `PageInfo`: Implementuje obsługa dialogów
### Kluczowe Funkcje
- `SetFlags()`: Aktualizuje Flags w stanie wewnętrznym lub konfiguracji
- `SetDialogFlags()`: Aktualizuje DialogFlags w stanie wewnętrznym lub konfiguracji
- `GetDialogFlags()`: Pobiera DialogFlags ze stanu wewnętrznego lub struktury danych
- `FirstButton()`: Wykonuje FirstButton operację jako część obsługa dialogów
- `SetFocus()`: Aktualizuje Focus w stanie wewnętrznym lub konfiguracji
- `DefaultButton()`: Wykonuje DefaultButton operację jako część obsługa dialogów
- `Control()`: Wykonuje Control operację jako część obsługa dialogów
- `SingleBox()`: Wykonuje SingleBox operację jako część obsługa dialogów
- `DoubleBox()`: Wykonuje DoubleBox operację jako część obsługa dialogów
- `Edit()`: Wykonuje Edit operację jako część obsługa dialogów
### Podsumowanie
Plik `FarDialogs.hpp` zapewnia podstawową funkcjonalność dla obsługa dialogów. Definiuje 5 klas(y) i zawiera około 10 funkcji wspierających operacje menedżera plików Far Manager.
