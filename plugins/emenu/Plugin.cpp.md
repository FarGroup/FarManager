# Plugin.cpp
## English
### Purpose
Main functional areas: plugin interface
### Key Classes
- `DiskMenuParam`: Implements plugin interface
- `SMenuDlgParam`: Implements plugin interface
- `output_suppressor`: Implements plugin interface
### Key Functions
- `IsWindowsVersionOrGreater()`: Tests whether WindowsVersionOrGreater condition is true or property exists
- `IsWindowsXPOrGreater()`: Tests whether WindowsXPOrGreater condition is true or property exists
- `settings()`: Updates tings in internal state or configuration
- `Builder()`: Executes Builder operation as part of plugin interface
- `oMainMenu()`: Executes oMainMenu operation as part of plugin interface
- `oDrivesMenu()`: Executes oDrivesMenu operation as part of plugin interface
- `szMenuItem()`: Executes szMenuItem operation as part of plugin interface
- `strDir()`: Executes strDir operation as part of plugin interface
- `szFile()`: Executes szFile operation as part of plugin interface
- `pCMenu2()`: Executes pCMenu2 operation as part of plugin interface
### Summary
The `Plugin.cpp` file provides essential functionality for plugin interface. It defines 3 class(es) and contains approximately 10 function(s) to support the Far Manager file manager operations.

---

## Polski (Polish)
### Cel
Główne obszary funkcjonalne: interfejs wtyczek
### Kluczowe Klasy
- `DiskMenuParam`: Implementuje interfejs wtyczek
- `SMenuDlgParam`: Implementuje interfejs wtyczek
- `output_suppressor`: Implementuje interfejs wtyczek
### Kluczowe Funkcje
- `IsWindowsVersionOrGreater()`: Testuje czy WindowsVersionOrGreater warunek jest prawdziwy lub właściwość istnieje
- `IsWindowsXPOrGreater()`: Testuje czy WindowsXPOrGreater warunek jest prawdziwy lub właściwość istnieje
- `settings()`: Aktualizuje tings w stanie wewnętrznym lub konfiguracji
- `Builder()`: Wykonuje Builder operację jako część interfejs wtyczek
- `oMainMenu()`: Wykonuje oMainMenu operację jako część interfejs wtyczek
- `oDrivesMenu()`: Wykonuje oDrivesMenu operację jako część interfejs wtyczek
- `szMenuItem()`: Wykonuje szMenuItem operację jako część interfejs wtyczek
- `strDir()`: Wykonuje strDir operację jako część interfejs wtyczek
- `szFile()`: Wykonuje szFile operację jako część interfejs wtyczek
- `pCMenu2()`: Wykonuje pCMenu2 operację jako część interfejs wtyczek
### Podsumowanie
Plik `Plugin.cpp` zapewnia podstawową funkcjonalność dla interfejs wtyczek. Definiuje 3 klas(y) i zawiera około 10 funkcji wspierających operacje menedżera plików Far Manager.
