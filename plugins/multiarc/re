\MultiArc
    тут находятся последние официальные сорцы (т.е. после
    последнего патча) + файлы: p.bat, pall.bat

\MultiArc\DIFF
    тут валяются официальные diff'ы

\MultiArc\DIFF.DOC
    тут описаловка к ним

\MultiArc\new
    тут в самом общем случае те же самые сорцы, что и
    каталогом выше, или мои наработки + файлы cc.bat,
    dif.bat, settitle.exe

\MultiArc\new\diff.my
    созданные мною патчи, до тех пор, пока они не войдут
    в официальные (т.е. не перейдут в ..\diff.doc)

\MultiArc\new\obj
    тут - промежуточные "ошметки" компиляции ;-)

\MultiArc\new\final
    сюда сыпятся *.dll, *.hlf, *.lng, *.fmt после компиляции

