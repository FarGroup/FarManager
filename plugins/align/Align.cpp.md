# Align.cpp
## English
### Purpose
Main functional areas: core functionality
### Key Functions
- `ReformatBlock()`: Executes ReformatBlock operation as part of core functionality
- `JustifyBlock()`: Executes JustifyBlock operation as part of core functionality
- `JustifyString()`: Executes JustifyString operation as part of core functionality
- `GetGlobalInfoW()`: Retrieves GlobalInfoW from internal state or data structure
- `SetStartupInfoW()`: Updates StartupInfoW in internal state or configuration
- `GetPluginInfoW()`: Retrieves PluginInfoW from internal state or data structure
- `OpenW()`: Opens resource and prepares it for access operations
- `settings()`: Updates tings in internal state or configuration
- `Builder()`: Executes Builder operation as part of core functionality
### Summary
The `Align.cpp` file provides essential functionality for core functionality. and contains approximately 9 function(s) to support the Far Manager file manager operations.

---

## Polski (Polish)
### Cel
Główne obszary funkcjonalne: podstawowa funkcjonalność
### Kluczowe Funkcje
- `ReformatBlock()`: Wykonuje ReformatBlock operację jako część podstawowa funkcjonalność
- `JustifyBlock()`: Wykonuje JustifyBlock operację jako część podstawowa funkcjonalność
- `JustifyString()`: Wykonuje JustifyString operację jako część podstawowa funkcjonalność
- `GetGlobalInfoW()`: Pobiera GlobalInfoW ze stanu wewnętrznego lub struktury danych
- `SetStartupInfoW()`: Aktualizuje StartupInfoW w stanie wewnętrznym lub konfiguracji
- `GetPluginInfoW()`: Pobiera PluginInfoW ze stanu wewnętrznego lub struktury danych
- `OpenW()`: Otwiera zasób i przygotowuje go do operacji dostępu
- `settings()`: Aktualizuje tings w stanie wewnętrznym lub konfiguracji
- `Builder()`: Wykonuje Builder operację jako część podstawowa funkcjonalność
### Podsumowanie
Plik `Align.cpp` zapewnia podstawową funkcjonalność dla podstawowa funkcjonalność. i zawiera około 9 funkcji wspierających operacje menedżera plików Far Manager.
