# TmpClass.hpp
## English
### Purpose
This file implements functionality related to: Temporary panel plugin class header file
Main functional areas: core functionality
### Key Classes
- `TmpPanel`: Implements core functionality
### Key Functions
- `TmpPanel()`: Executes TmpPanel operation as part of core functionality
- `GetFindData()`: Retrieves FindData from internal state or data structure
- `GetOpenPanelInfo()`: Retrieves OpenPanelInfo from internal state or data structure
- `SetDirectory()`: Updates Directory in internal state or configuration
- `PutFiles()`: Executes PutFiles operation as part of core functionality
- `BeginPutFiles()`: Executes BeginPutFiles operation as part of core functionality
- `CommitPutFiles()`: Executes CommitPutFiles operation as part of core functionality
- `PutDirectoryContents()`: Executes PutDirectoryContents operation as part of core functionality
- `PutOneFile()`: Executes PutOneFile operation as part of core functionality
- `SetFindList()`: Updates FindList in internal state or configuration
### Summary
The `TmpClass.hpp` file provides essential functionality for core functionality. It defines 1 class(es) and contains approximately 10 function(s) to support the Far Manager file manager operations.

---

## Polski (Polish)
### Cel
Ten plik implementuje funkcjonalność związaną z: Temporary panel plugin class header file
Główne obszary funkcjonalne: podstawowa funkcjonalność
### Kluczowe Klasy
- `TmpPanel`: Implementuje podstawowa funkcjonalność
### Kluczowe Funkcje
- `TmpPanel()`: Wykonuje TmpPanel operację jako część podstawowa funkcjonalność
- `GetFindData()`: Pobiera FindData ze stanu wewnętrznego lub struktury danych
- `GetOpenPanelInfo()`: Pobiera OpenPanelInfo ze stanu wewnętrznego lub struktury danych
- `SetDirectory()`: Aktualizuje Directory w stanie wewnętrznym lub konfiguracji
- `PutFiles()`: Wykonuje PutFiles operację jako część podstawowa funkcjonalność
- `BeginPutFiles()`: Wykonuje BeginPutFiles operację jako część podstawowa funkcjonalność
- `CommitPutFiles()`: Wykonuje CommitPutFiles operację jako część podstawowa funkcjonalność
- `PutDirectoryContents()`: Wykonuje PutDirectoryContents operację jako część podstawowa funkcjonalność
- `PutOneFile()`: Wykonuje PutOneFile operację jako część podstawowa funkcjonalność
- `SetFindList()`: Aktualizuje FindList w stanie wewnętrznym lub konfiguracji
### Podsumowanie
Plik `TmpClass.hpp` zapewnia podstawową funkcjonalność dla podstawowa funkcjonalność. Definiuje 1 klas(y) i zawiera około 10 funkcji wspierających operacje menedżera plików Far Manager.
