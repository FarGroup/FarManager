# FileCase.hpp
## English
### Purpose
Main functional areas: file system operations
### Key Classes
- `Options`: Implements file system operations
### Key Functions
- `CaseConvertion()`
- `CaseWord()`
- `IsCaseMixed()`
- `ProcessName()`
### Summary
The `FileCase.hpp` file provides essential functionality for file system operations. It defines 1 class(es) and contains approximately 4 function(s) to support the Far Manager file manager operations.

---

## Polski (Polish)
### Cel
Główne obszary funkcjonalne: operacje systemowyu plików
### Kluczowe Klasy
- `Options`: Implementuje operacje systemowyu plików
### Kluczowe Funkcje
- `CaseConvertion()`
- `CaseWord()`
- `IsCaseMixed()`
- `ProcessName()`
### Podsumowanie
Plik `FileCase.hpp` zapewnia podstawową funkcjonalność dla operacje systemowyu plików. Definiuje 1 klas(y) i zawiera około 4 funkcji wspierających operacje menedżera plików Far Manager.
